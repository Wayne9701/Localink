# Skills boundary

Localink supports Skill contracts, discovery and bounded reads, but does not own the shared Skill library.

Shared Skills remain canonical outside Localink and may be consumed by Codex, Cursor, Localink and other compatible clients.

Localink should provide only the runtime/adapter required to discover and read configured Skills. Skill content is distributed separately through the AI Environment Bootstrap when needed.
